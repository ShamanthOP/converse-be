import userSchema from "./user";
import conversationSchema from "./conversation";
import messageSchema from "./message";

const schema = [userSchema, conversationSchema, messageSchema];

export default schema;
